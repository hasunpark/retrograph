import { ChartOptions } from "../types";

export function createScatterPlot(
  data: [number, number][],
  options: ChartOptions
): string {
  // Implementation for scatter plot rendering
  return "Scatter plot not implemented";
}
